<?xml version="1.0"?>

<!--
 The contents of this file are subject to the terms
 of the Common Development and Distribution License
 (the "License").  You may not use this file except
 in compliance with the License.
 
 You can obtain a copy of the license at
 https://jwsdp.dev.java.net/CDDLv1.0.html
 See the License for the specific language governing
 permissions and limitations under the License.
 
 When distributing Covered Code, include this CDDL
 HEADER in each file and include the License file at
 https://jwsdp.dev.java.net/CDDLv1.0.html  If applicable,
 add the following below this CDDL HEADER, with the
 fields enclosed by brackets "[]" replaced with your
 own identifying information: Portions Copyright [yyyy]
 [name of copyright owner]
-->


<project name="JAXWS-RI Runtime" default="compile" basedir=".">

    <!-- default is false. use "main-debug" target in top level file
         for debugging information -->
    <property name="compile.debug" value="false"/>

    <!-- Should Java compilations set the 'deprecation' compiler option? -->
    <property name="compile.deprecation" value="false"/>

    <!-- Should Java compilations set the 'optimize' compiler option? -->
    <property name="compile.optimize" value="true"/>

  <property name="build.dir" value="${basedir}/build"/>
  
  <target name="prepare">
    <mkdir dir="${build.dir}/classes"/>
  </target>

  <target name="compile" depends="prepare">
    <javac srcdir="src"
        destdir="${basedir}/build/classes"
        debug="${compile.debug}"
        deprecation="${compile.deprecation}"
        optimize="${compile.optimize}"
        source="1.5">
      <classpath refid="runtime.compile.classpath"/>
    </javac>
  </target>

  <target name="compile-mustang" depends="prepare">
    <javac srcdir="src"
        destdir="${basedir}/build/classes"
        debug="${compile.debug}"
        deprecation="${compile.deprecation}"
        optimize="${compile.optimize}"
        source="1.5"
        excludes="**/JaxWsRtObjectFactory*.java,**/WSServletContext.java,**/WSDLPublisher.java,**/servlet/,**/ServletDelegate.java, **/ServletSecondDelegate.java, **/FastInfoset*.java, **/SourceReaderFactory.java">
      <classpath refid="runtime.compile.classpath"/>
    </javac>
  </target>
  
    <target name="clean">
    <delete dir="${build.dir}"/>
  </target>

</project> 
