<descriptor since="2.0" transport="http">
  <description>This is a Provider test</description>
  <client name="provider.doclit.context"sideEffectFree="true"><![CDATA[
    req = new VoidType();
    response = helloPort.voidTest(req);
    assertNotNull(response);

    map = ((BindingProvider)stub).getResponseContext().get(MessageContext.HTTP_RESPONSE_HEADERS);

    assertNotNull(map);
    assertEquals("bar", map.get("foo").get(0));

  ]]></client>

  <service basedir="./server">
    <wsdl href="hello_literal.wsdl"/>
  </service>

</descriptor>
