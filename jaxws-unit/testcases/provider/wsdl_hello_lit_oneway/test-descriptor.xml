<descriptor since="2.0">
  <description>This is a Provider Oneway test</description>
  <client name="provider.doclit.oneway" sideEffectFree="true"><![CDATA[
    req = new Hello_Type();
    req.setArgument("foo");
    req.setExtra("bar");
    for(i=0; i < 5; i++) {
        helloPort.hello(req);
    }
  ]]></client>

  <service basedir="./server">
    <wsdl href="hello_literal_oneway.wsdl"/>
  </service>

</descriptor>
