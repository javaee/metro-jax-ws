p1 = new Person();
p1.setFirstname("f");
p1.setLastname("l");

o = new ArrayList();
o.add(p1);

h = new Holder(o);
docLitBarePort.echo(h);
assertTrue(h.value.size()==o.size());

r=docLitWrappedPort.echo(o);
assertTrue(r.size()==o.size());
